---
description: 1論点について機械が知っている事実を全部出す。質問文を組み立てる前や、定例前の下調べに
argument-hint: <論点ID> [プロジェクトのパス]
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/reqmap-cli:*) Read
---

!`${CLAUDE_PLUGIN_ROOT}/scripts/reqmap-cli show "$1" --root="${2:-.}"`

上の事実だけを材料に、相手に出せる質問文を組み立ててください。組み立て方は決まっています。

> いま決まっていないこと → それが決まらないと何が止まるか（下流） → 選択肢 → いつまでに欲しいか（回答が要る日）

- 「止まるもの」は **■ 下流** から、期限は **■ 日付と影響度** から取る。推測で足さない
- **■ やりとり** に出した記録があれば「再送」として書く。何度も聞き直しているなら問いの立て方を変える
- **■ 仕様側の関連** があれば、決まったあと仕様に写す箇所として伝える
- **■ 観点** に矛盾（fsl.forbidden_accepted・graph.conflict）があれば、質問より先にそれを伝える
